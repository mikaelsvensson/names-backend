package info.mikaelsvensson.babyname.service.util.metrics;

public enum MetricEvent {
    EMAIL_SENT,
    LOG_IN,
    USER_ADDED,
    RELATIONSHIP_ADDED,
    ACTIVE_USER,
    VOTE,
    HTTP_OK,
    HTTP_CLIENT_ERROR,
    HTTP_SERVER_ERROR,
    HTTP_ADMIN,
    HTTP_OTHER
}
